spring:
  application:
    name: Spt-Auth
  cloud:
    nacos:
      config:
        server-addr: 10.67.78.55:8003
        group: DEFAULT_GROUP
        prefix: spt-auth
        file-extension: yaml
      discovery:
        server-addr: 10.67.78.55:8003
  thymeleaf:
    cache: false

logging:
  level:
    org:
      springframework:
        boot:
          actuate:
            endpoint:
              EndpointId: error
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error
